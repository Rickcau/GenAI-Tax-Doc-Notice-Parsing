{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run/Debug Blob Trigger Function (Azure Storage)",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:azureFunctions.pickProcess}",
            "justMyCode": true,
            "preLaunchTask": "func: host start blob trigger"
        },
        {
            "name": "Attach to .NET Functions",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:azureFunctions.pickProcess}"
        }
    ]
}